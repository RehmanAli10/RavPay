import React, {memo} from 'react';
import TabNavigation from '../../../utilities/TabNavigation';

const HomeNavigationContainer = memo(() => {
  return <TabNavigation />;
});

export default HomeNavigationContainer;
